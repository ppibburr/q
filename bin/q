#!/usr/bin/env ruby
require 'rubygems'

begin
  require 'q'
rescue LoadError
  $: << File.dirname(File.dirname(__FILE__)) + '/lib'
  require 'q'
end

code = File.read(ARGV[0])

QSexp.build(code).write
out = QSexp::BUFFER.join

puts out
